name: saferefine
channels:
  - conda-forge
dependencies:
  - python=3.12
  - numpy>=1.24
  - pandas>=2.0
  # --- Tier 2 (full pipeline) extras; uncomment to build datasets / train hosts ---
  # - pytorch>=2.1
  # - opencv
  # - matplotlib
  # - pillow
  # - nibabel
